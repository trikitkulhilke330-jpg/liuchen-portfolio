# 部署说明 —— 刘晨个人作品集网站

> 这份文件是给 **Claude Code** 看的操作指令，也方便你（站主）了解流程。
> 目标：把这个 `site/` 文件夹部署成一个带自定义域名、HTTPS 的正式个人网站。

---

## 一、这个网站是什么

- 纯静态前端网站，**没有后端、没有数据库、不需要安装依赖、不需要构建（build）**。
- 入口文件就是 `site/index.html`，浏览器直接打开即可运行。
- 运行时（`support.js`）、两个交互组件（`Ballpit.jsx` 球池、`MagicBento.jsx`）、以及 `public/` 里的图片都是相对路径引用，整个文件夹原样上传即可。
- 字体、GSAP、three.js、ogl 等通过 CDN 在运行时加载，**访客需要联网**。

```
site/
├── index.html        ← 网站入口（静态主机默认加载它）
├── support.js        ← 运行时，勿改
├── Ballpit.jsx       ← 联系页背景球池
├── MagicBento.jsx    ← 个人优势组件
└── public/           ← 头像与作品图
    ├── portrait.png
    ├── work-1.png … work-4.png
```

> ⚠️ 不要改动 `index.html` 里对 `./support.js`、`./Ballpit.jsx`、`./MagicBento.jsx`、`public/*` 的相对路径。直接整文件夹部署最稳。

---

## 二、需要站主（人）先完成的两件事

这两步涉及**付款和账号注册，Claude Code 无法代办**：

1. **注册一个 Vercel 账号**（免费）：https://vercel.com → 用 GitHub 账号登录最省事。
2. **买一个域名**（约 ¥70–90/年）。推荐：
   - **Cloudflare Registrar**（按成本价卖，最便宜，且解析快）
   - 或 **Namecheap** / **阿里云万网**
   买完先放着，第三步会用到。

> 如果暂时不想买域名，可以跳过，先部署拿到一个免费的 `xxx.vercel.app` 网址，之后随时再绑域名。

---

## 三、给 Claude Code 的部署指令

请在本地终端里，于 `site/` 文件夹**所在的上级目录**执行。

### 步骤 1：部署到 Vercel（拿到免费网址）

```bash
# 安装 Vercel 命令行工具（只需一次）
npm i -g vercel

# 进入网站文件夹
cd site

# 登录（会打开浏览器，用第二步注册的账号登录）
vercel login

# 首次部署：一路回车用默认值即可
#   - Set up and deploy? → Y
#   - Which scope? → 选你的账号
#   - Link to existing project? → N
#   - Project name? → liuchen-portfolio（或任意）
#   - In which directory is your code? → ./（当前目录，回车）
#   - 覆盖构建设置? → N
vercel

# 部署到正式环境
vercel --prod
```

完成后终端会输出一个网址，例如 `https://liuchen-portfolio.vercel.app` —— 这时网站已经上线，可以访问了。

### 步骤 2：绑定自定义域名

假设站主买的域名是 `liuchen.design`（替换成真实域名）：

```bash
# 在 site 目录下执行
vercel domains add liuchen.design
# 或在项目里添加：
vercel domains add liuchen.design liuchen-portfolio
```

执行后 Vercel 会提示需要配置的 DNS 记录。**两种情况：**

**A. 域名在 Cloudflare / Namecheap 等注册商（推荐，最简单）**
登录域名注册商后台 → DNS 设置 → 按 Vercel 提示添加：
- 根域名 `liuchen.design` → 添加一条 **A 记录**，指向 `76.76.21.21`
- `www.liuchen.design` → 添加一条 **CNAME 记录**，指向 `cname.vercel-dns.com`

**B. 想把整个域名 DNS 托管给 Vercel**
把注册商处的 **Nameserver（域名服务器）** 改成 Vercel 提示的两个地址（如 `ns1.vercel-dns.com` / `ns2.vercel-dns.com`）。

### 步骤 3：等待生效

- DNS 通常几分钟到几小时生效（最长 48 小时）。
- Vercel 会**自动签发免费 HTTPS 证书**，无需手动操作。
- 用 `vercel domains inspect liuchen.design` 可查看绑定状态。

生效后访问 `https://liuchen.design` 即是正式个人网站。✅

---

## 四、以后想更新网站内容

改完文件后，在 `site/` 目录重新运行一行即可：

```bash
vercel --prod
```

---

## 五、备选：不用命令行的纯网页操作

如果不想用终端，也可以：
1. 把 `site/` 文件夹拖到 https://vercel.com/new 的上传区（或先传到 GitHub 仓库再 import）。
2. 部署成功后，在 Vercel 项目页 **Settings → Domains** 里填入你的域名，按提示在注册商配置 DNS。

效果完全一样。

---

## 常见问题

- **打开页面白屏 / 特效不出来？** 检查是否整文件夹一起上传（缺了 `support.js` 或 `.jsx` 会白屏），以及访客是否联网（字体和特效库走 CDN）。
- **图片不显示？** 确认 `public/` 文件夹一并上传，且大小写与 `index.html` 中引用一致。
- **想换域名 / 加二级域名？** 重复第三步即可，一个项目可绑多个域名。
